package:
  name: qualicati

components:
  - qualicati

internal-dependencies:
  - soma
  - anatomist

requirements:
  build:
    - ${{ compiler('cxx') }}

  run:
    - nibabel
    - pydicom
    - scikit-image
    - pandas
    - pycryptodome
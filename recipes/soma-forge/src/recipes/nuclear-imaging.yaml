package:
  name: nuclear-imaging

components:
  - nuclear_imaging-nonfree
  - nuclear_imaging-gpl

internal-dependencies:
    - soma
    - anatomist

requirements:
  build:
    - ${{ compiler('cxx') }}
